using UnityEngine;
using System.Collections;

public class Drop : Pattern {
	void FixedUpdate()
	{
		/*if(count > 0.1f && canFire)
		{
			float angle = Random.Range(60,120);
			GameObject bulletRef = BulletPool.current.EnableBullet(transform.position, angle, 1f, 4);
			ChangeAfter.Create(bulletRef, 2f, angle, 1.2f);

			numBullets++;
			count = 0;
		}

		base.FixedUpdate ();*/
	}
}
